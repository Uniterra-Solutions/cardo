# Acceptance — Issue 2, 5, 15

One verifiable criterion per functional requirement (prd.md). Evidence is objective: a test (PBT/unit), a command output, or a documented observable behaviour.

---

## Part A — Issue 2: universal settings UI

| Req    | Acceptance criterion                                                                                                                                                                                                                                                                                                                                                                       | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-2.1 | The uniterra-settings-ui workspace built-in is declared in the registry and appears in expectedBuiltinBundles(); provisioning copies it into the profile (workspace kind, version-identity staleness).                                                                                                                                                                                     | builtin-pbt REGISTRY property passes with the new workspace entry; builtin-pbt WORKSPACE-STALENESS property asserts copyBuiltinsStale() re-provisions the workspace row on version mismatch (version-identity staleness); `pnpm --filter @uniterra-solutions/uniterra-desktop test` green.                                                                                                                                                                     |
| FR-2.2 | The settings UI enumerates every registered dsh-settings namespace (e.g. llm-uniterra) via describe() with its serialized schema.                                                                                                                                                                                                                                                          | Unit test: host describe → the descriptor list contains EVERY registered namespace (llm-uniterra included), each with schema.toJSON() non-null; PBT over arbitrary descriptor lists asserting the property explicitly: for any descriptor list, describe() preserves every entry's ns and non-null schema.toJSON().                                                                                                                                            |
| FR-2.3 | A namespace's plain fields render as the right controls from schema metadata (string/number/boolean/enum/nested/array; default+description shown).                                                                                                                                                                                                                                         | Unit test: schema-to-field-tree mapper over schemastery fixtures (incl. enums, nested objects, arrays, pattern-string → pattern-validated text input); UI manual QA: open the settings page in the Electron shell and screenshot every field type (pixel-checked per uniterra-qa).                                                                                                                                                                             |
| FR-2.4 | Saving a form writes through update/replace/mutate; the stored document round-trips valid against the same schema; revision guard rejects stale writes.                                                                                                                                                                                                                                    | Unit test: patch → update payload validates against the schema; update/mutate round-trip assertions write and re-read a document valid against the same schema; replace round-trip asserts the same for the replace endpoint; revision-mismatch write is refused (SETTINGS_CONFLICT passed through).                                                                                                                                                           |
| FR-2.5 | The provider's complex fields render via registered widgets (model catalog capacity/interrogation, write-only API key, models.dev params); proxy stays a plain nested object (enabled + url) rendered by the generic form, not a widget.                                                                                                                                                   | Manual QA in the Electron shell (screenshot each widget); widget registry unit test: registration + override resolution.                                                                                                                                                                                                                                                                                                                                       |
| FR-2.6 | The provider no longer registers its own settings.section page; its namespace renders inside the generic UI; UniterraSection.tsx is deleted.                                                                                                                                                                                                                                               | grep: no settings.section registration remains in packages/uniterra-provider; file-existence check: packages/uniterra-provider/src/client/UniterraSection.tsx no longer exists; app boots and the provider's settings live under the generic Integrations page.                                                                                                                                                                                                |
| FR-2.7 | An extension with no registered settings namespace shows a read-only notice (no schema-configurable settings) with an optional open-profile-settings-document action (the settings seam's escape hatch — surfaced only for extensions with no schema-configurable settings; edits there can override form-managed sections; the form re-reads on next describe), instead of a broken page. | Unit test: the discovery filter (over the host-enumerated extension inventory) returns the read-only notice for an extension ABSENT from describe() — the document open affordance (hasDocument/openDocument on the settings UI's own host bridge) opens the profile settings document; a subsequent describe() re-read reflects document edits made via the escape hatch (the form re-reads on the next describe); manual QA with a non-registering built-in. |

## Part B — Issue 5: dsh-memory built-in

| Req    | Acceptance criterion                                                                                                                                                                                                 | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-5.1 | The npm spec @meomeo-dev/dsh-memory@0.5.6 is registered (exact pin, no caret).                                                                                                                                       | builtin-pbt 'npm specs are pinned exact' property; grep builtin.ts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| FR-5.2 | expectedBuiltinBundles()/hasAllBuiltins() include @meomeo-dev/dsh-memory; EXTRACT property covers the scoped-name path.                                                                                              | `pnpm --filter @uniterra-solutions/uniterra-desktop test` green (REGISTRY + EXTRACT properties).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| FR-5.3 | Provisioning works without a DEEPSEEK_API_KEY: the profile bundle row is present and the plugin loads at boot.                                                                                                       | Clean-container replay (scripts/verify-cli-container/run.sh, CI) asserts the full bundle set + real dsh boot; on this machine: `pnpm --filter @uniterra-solutions/uniterra-desktop test` green — the builtin-pbt REGISTRY property asserts the @meomeo-dev/dsh-memory@0.5.6 npm spec is registered (10 npm specs, pinned exact) and expectedBuiltinBundles()/hasAllBuiltins() cover it; real dsh boot with the provisioned bundle row is covered by the clean-container replay (CI). Keyless-boot note: warmupOnStart: true is local-only (boot warmup partitions memory sources — no model call, verified against the pinned tarball), so keyless boot is quiet; e2e memory tests still need a live key. |
| FR-5.4 | The provider-default remap is documented (/lmemory config set provider uniterra) in the docs/changelog, along with the model/reviewModel gateway-catalog requirement (defaults deepseek-v4-flash / deepseek-v4-pro). | grep the docs for the command and the default model ids; docs build/format clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| FR-5.5 | No native deps → no allowBuilds change; /lmemory ui panel renders in the Electron shell.                                                                                                                             | `git diff pnpm-workspace.yaml` shows no allowBuilds additions; manual QA: panel renders; recall works with a live key — record the manual steps in docs/modules/uniterra-desktop.md.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

## Part C — Issue 15: update progress overlay

| Req     | Acceptance criterion                                                                                                                                                                                                                                                                                                                                                                                                                | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-15.1 | Consent immediately shows an 'Initializing update...' message with a do-not-close hint before any work.                                                                                                                                                                                                                                                                                                                             | Manual QA: trigger Update Now → overlay appears instantly with the Phase-1 message; unit test on the overlay state machine's init event.                                                                                                                                                                                                                                                                                                                            |
| FR-15.2 | During the run the overlay continuously shows a spinner + the current stage label derived from CLI output.                                                                                                                                                                                                                                                                                                                          | parseUpdateProgress unit test maps representative CLI stage lines to overlay labels (unconditional — the helper is part of the decision surface); manual QA during a real update maps the live CLI stage lines to overlay labels.                                                                                                                                                                                                                                   |
| FR-15.3 | The process ends with an explicit success ('Update to <v> completed. Restarting Uniterra...') or failure (error + guidance) message — never a silent frozen state.                                                                                                                                                                                                                                                                  | Manual QA both paths; state-machine unit test asserts terminal states are always reached and rendered.                                                                                                                                                                                                                                                                                                                                                              |
| FR-15.4 | The updater is spawned as a child process (not detached) with --no-open while the app stays alive. macOS only (per PRD FR-15.4/15.5); Windows retains the existing quit-first flow.                                                                                                                                                                                                                                                 | update-overlay.test.mts unit test: updateInvocation(cmd, { noOpen: true }) returns the command + args ['update', '--no-open'] for the overlay flow; S4: desktop updater-flow unit assertion on the overlay spawn options (detached: false, stdio piped) + grep main.ts: the overlay update spawn uses no detached/stdio-ignore and the app is not quit before the child exits.                                                                                      |
| FR-15.5 | After success the app relaunches via app.relaunch()/app.quit() and the updated version boots; no second instance error. macOS only — on Windows the restart happens via the updater after the quit-first flow.                                                                                                                                                                                                                      | Manual QA: end-to-end update → app restarts on the new version, single instance holds. Local alternative: a fixture stub (a script that prints a done line with a version and exits 0) substituted for the updater command via env override, exercising the overlay success path (relaunch + single instance) without a real download; the success path shows the completion message, then app.relaunch() immediately followed by app.quit() with no await between. |
| FR-15.6 | On failure the app does not quit; the overlay shows the error with retry/dismiss; closing mid-update is blocked (the close is ignored while the update is running). An interrupted/killed update is treated as failure: the overlay offers retry, and re-running the updater reinstalls from scratch (copyInstalled is non-atomic — rm destination then ditto — so an interrupt may leave no working app, which the retry repairs). | Manual QA: override the updater command via env var (UNITERRA_UPDATE_COMMAND) to a fixture script that exits non-zero → overlay error state, app alive, retry/dismiss work; window close during the run is blocked (close does nothing until the update finishes); killing the update child mid-run → overlay failure state with retry, and re-running the updater reinstalls from scratch.                                                                         |

---

## Regression gates (all three issues)

- `pnpm run lint` and `pnpm run typecheck` clean (build first — tsc -b --noEmit fails on stale projects).
- Package test suites green: desktop (builtin-pbt + updater-flow), provider (build + dual-protocol + reasoning regressions), updater (decision), skills (provisioning).
- After touching uniterra-provider / new settings-ui: `pnpm run build` so lib/ (provider) and dist/ (workspace built-ins) are current for profile provisioning.
- After touching the install flow: scripts/verify-cli-container/run.sh (CI).
- CHANGELOG.md and affected docs updated; no default exports added; no new dependencies in package.json beyond the approved list (issue 15's ora/cli-progress is optional and gated on approval — diff-checkable after the fact).
