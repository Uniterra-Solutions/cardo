{
  "execution_mode": "serial",
  "batches": [
    [
      {
        "id": "mode-core",
        "task_prompt": "Implement the cardo mode registry core (standard | plan | debug) in packages/jovaltus. The failing PBT specs already on disk define the contract — run `pnpm --filter @cardo/jovaltus test:pbt` to see them red (TS2307: cannot find dist/mode.js). Create src/mode.ts with these NAMED exports (pure, no pi imports): `CardoMode = 'standard' | 'plan' | 'debug'`, `ToolSetState = { active: readonly string[]; baseBeforePlan: readonly string[] | undefined }`, `MODE_CYCLE = ['standard','plan','debug']`, `nextMode(mode)` (cycle successor, total), `modeToStatusText(mode)` -> 'standard' | 'plan mode' | 'debug mode', `statusTextToMode(text)` (total inverse, unknown -> 'standard'), `modeEntryWrite(mode)` -> { mode }, `modeEntryRead(data)` (total: { mode } wins; legacy { enabled: true } -> 'plan', { enabled: false }/missing/garbage -> 'standard'; never throws), `togglePlanMode(mode)` (plan<->standard, debug->plan), `toggleDebugMode(mode)` (debug<->standard, plan->debug), `restoreMode(flags: { plan: boolean; debug: boolean }, persisted: CardoMode | undefined)` (persisted wins; else plan flag -> 'plan'; else debug flag -> 'debug'; else 'standard'), `DEBUG_MODE_NOTE` (exact FR-7 English note — see the prompt test in mode.test.mts for the byte-exact text), `debugPromptAppend(systemPrompt)` (appends \\n\\n + DEBUG_MODE_NOTE), `applyModeTools(state, mode, planTools)` (mode 'plan': baseBeforePlan = baseBeforePlan ?? active, active = base + planTools; else active = base minus planTools, baseBeforePlan reset to undefined). Then refactor src/plan-mode.ts into the shared mode wiring: keep every existing export byte-for-byte (PLAN_MODE_TOOLS, JOVALTUS_MODE_STATUS_KEY, JOVALTUS_EXECUTE_WIDGET_KEY, JOVALTUS_EXECUTE_STATUS_KEY, ExecuteWidgetState, buildExecuteWidgetLines, planExecuteWidgetInitial/AgentStart/AgentDone/Done, the widget transition functions). registerPlanMode(pi, controller) gains a shared ModeController (created once in the factory, owns `mode` + `toolsBeforePlan`); it keeps the /planmode command (now togglePlanMode), shift+p shortcut, plan-mode flag, the tool_call gate (blocks plan/execute_plan iff mode !== 'plan'), the plan before_agent_start note (iff mode === 'plan'), and the SINGLE session_start restore handler (both flags + last persisted 'jovaltus-mode' entry via modeEntryRead -> restoreMode -> controller). Add registerDebugMode(pi, controller) in the same module: 'debug-mode' flag (boolean, default false), /debugmode command (toggleDebugMode), and a debug before_agent_start handler (iff mode === 'debug', appending DEBUG_MODE_NOTE; base prompt preserved as prefix). Wire ModeController + both registrations in src/index.ts (entry stays a default-exported factory). Notify strings per toggle transition are asserted exactly in test/pbt/debug-mode.test.mts (expectedNotify) — standard->debug, debug->standard, plan->debug, debug->plan (via /planmode), plan<->standard. Status text pushed under JOVALTUS_MODE_STATUS_KEY via modeToStatusText; persistence via pi.appendEntry('jovaltus-mode', modeEntryWrite(mode)). Gate the plan note exactly as today. Run `pnpm run build` then `pnpm --filter @cardo/jovaltus test:pbt` until the whole suite (mode.test.mts, debug-mode.test.mts, plan-mode.test.mts and all existing files) is green. Lint the package with `pnpm --filter @cardo/jovaltus lint` (max-warnings 0). No new dependencies, no `any` types, .js extensions on internal imports."
      }
    ],
    [
      {
        "id": "desktop-mode-ui",
        "task_prompt": "Implement the desktop three-state mode UI in vendor/pi-gui/apps/desktop (every change marked with a `// Cardo:` comment, minimal vendored edits only). The jovaltus extension was just built by the previous agent and now reports JOVALTUS_MODE_STATUS_KEY with text 'standard' | 'plan mode' | 'debug mode' (read from the live 'jovaltus-mode' status exactly like today's plan-mode flow, guarded by the same jovaltusAvailable check). Changes: (1) src/App.tsx — classify the jovaltus-mode status text into the three states instead of `text === \"plan mode\"`, and make the composer's mode toggle cycle standard -> plan -> debug -> standard by submitting the mapped runtime command (/planmode for standard->plan, /debugmode for plan->debug and debug->standard) through the existing guarded resolveRuntimeSlashCommand + submitComposer path (silent — no optimistic timeline row, per app-store-composer's generic suppression). (2) src/jovaltus-ui.tsx — JovaltusModeButton props become { mode, onToggle } with label = current mode name, aria-pressed = mode !== 'standard', title describing the cycle, data-testid=\"jovaltus-mode-button\" kept. (3) src/composer-surface.tsx + src/composer-panel.tsx — rename the `planModeOn: boolean` prop to a three-value `mode`. (4) src/new-thread-view.tsx — the mode picker gains a third option (standard/plan/debug), data-testids new-thread-mode-standard / -plan / -debug, default standard. (5) src/hooks/use-new-thread-controller.tsx — mode state default 'standard', reset to 'standard' in resetSurface, passed into startThread. (6) src/desktop-state.ts — StartThreadInput.jovaltusMode?: boolean becomes mode?: 'plan' | 'debug'. (7) electron/app-store-worktree.ts — startThread runs /planmode or /debugmode silently (suppressOptimisticMessage: true) before the first message based on input.mode, mirroring the existing jovaltus path. Do NOT change electron/app-store-composer.ts (the silent path is already generic) or src/extension-session-ui.tsx (jovaltus-mode is already in CUSTOM_RENDERED_STATUS_KEYS). No new tokens needed (reuse existing styles). Update the two live contract specs to the three-state behavior: tests/live/jovaltus-mode-toggle.spec.ts (button starts standard aria-pressed=false, click -> plan, click -> debug, click -> standard; shift+tab cycles identically; /debugmode and /planmode both stay silent — no timeline row, no generic dock bar) and tests/live/jovaltus-new-thread-mode.spec.ts (picker shows three options, default standard, reset on surface reset; choosing debug starts the thread in debug mode with no /debugmode timeline row and the created thread's mode button reads debug immediately). Verify: `pnpm --filter @pi-gui/desktop build`, `pnpm --filter @pi-gui/desktop typecheck`, `pnpm --filter @pi-gui/desktop test:pbt` (must stay green — no desktop PBT changes), then run the two specs from the desktop dir: PI_APP_TEST_MODE=background PI_OFFLINE=1 PI_APP_DISABLE_CARDO_UPDATE_CHECK=1 node_modules/.bin/playwright test -c playwright.config.ts tests/live/<file> (requires built out/ + dist/, and the jovaltus extension built by the previous agent)."
      }
    ],
    [
      {
        "id": "verify",
        "task_prompt": "Final verification of the debug-mode feature. From the repo root run: `pnpm run lint` and `pnpm run typecheck` (max-warnings 0 — any warning is a release blocker; fix violations in the changed packages only), `pnpm --filter @cardo/jovaltus test:pbt` (must be green), and `pnpm --filter @pi-gui/desktop build` + `pnpm --filter @pi-gui/desktop typecheck` + `pnpm --filter @pi-gui/desktop test:pbt`. Spot-check the diff for: no `any` types, no new dependencies in package.json, .js extensions on internal imports, no default exports outside the pi extension entry factory, and every desktop change carrying a `// Cardo:` marker. Verify the debug-mode system prompt note ([DEBUG MODE] ... 3-step workflow) matches the FR-7 text in the PRD. Report a concise final summary of what changed."
      }
    ]
  ]
}
