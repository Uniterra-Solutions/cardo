# Build output — regenerated by `pnpm run build` (tsc + esbuild), never committed.
# The desktop's workspace built-in provisioning copies lib/ into the profile at
# runtime from a build that ran during `uniterra setup`, so only src/ ships.
lib/
